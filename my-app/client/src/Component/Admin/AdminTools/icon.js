import { Icon } from 'react-icons-kit';
import { cog } from 'react-icons-kit/fa/cog'
import { remove } from 'react-icons-kit/fa/remove'

export const IconRemove = (props) => <Icon size={props.size} icon={remove} />;
export const IconCog = (props) => <Icon size={props.size} icon={cog} />;
