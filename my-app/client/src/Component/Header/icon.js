import { Icon } from "react-icons-kit";
import { search } from "react-icons-kit/fa/search";
import { envelope } from 'react-icons-kit/fa/envelope'


export const IconSearch = (props)=><Icon size={props.size} icon={search} />
export const IconVnvelope = (props)=><Icon size={props.size} icon={envelope} />
